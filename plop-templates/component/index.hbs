import React from 'react'
import { View } from '@tarojs/components'

const {{ properCase name }} = React.memo(() => {
  return (
    <View className='{{ name }}'>{{ properCase name }}</View>
  )
})

export default {{ properCase name }}