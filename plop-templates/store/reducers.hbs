import initState from '../state/{{ name }}'
import { DEMO } from '../constants/{{ name }}'

export default function counter(state = initState, action) {
  switch (action.type) {
    case DEMO:
      return {
        ...state
      }
    default:
      return state
  }
}
